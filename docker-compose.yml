services:
  es-levels:
    image: dnwong/saul-es-levels:latest
    container_name: es-levels
    ports:
      - "8080:8080"
    restart: unless-stopped
    environment:
      - TWELVEDATA_API_KEY=${TWELVEDATA_API_KEY}
